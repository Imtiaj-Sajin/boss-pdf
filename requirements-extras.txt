# Optional extras for advanced extraction & OCR.
# run.bat installs each line independently — failures are non-fatal.
# Anything that won't install on your Python version is skipped silently
# and the converter falls back to whatever IS available.

# Page rendering (for OCR). Pure-pip, no Poppler needed.
PyMuPDF>=1.24

# Table extraction for bordered/borderless tables in native PDFs.
camelot-py[cv]>=0.11.0
opencv-python-headless>=4.10

# OCR for scanned PDFs — RapidOCR uses ONNX runtime (lightweight, broad Python support).
# Ports the same models PaddleOCR uses, no paddlepaddle dependency.
rapidocr-onnxruntime>=1.3

# Tesseract fallback (only used if everything above fails).
# Requires system Tesseract binary; install is optional.
pytesseract>=0.3.13
pdf2image>=1.17
