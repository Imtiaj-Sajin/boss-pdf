# Optional extras. run.bat installs each line independently — failures are
# non-fatal so a missing wheel for one package doesn't kill the install.

# ---- Page rendering (no system Poppler dependency) ----
PyMuPDF>=1.24

# ---- Bordered/borderless table extraction for NATIVE PDFs ----
camelot-py[cv]>=0.11.0
opencv-python-headless>=4.10

# ---- PRIMARY OCR: PaddleOCR 3.x with PP-StructureV3 ----
# Current SOTA for layout + table OCR. Produces structured HTML tables so
# row/column positions are preserved. REQUIRES Python 3.8 – 3.13 (NOT 3.14).
# If your Python is too new, install Python 3.13 from python.org.
paddlepaddle>=3.0
paddleocr>=3.0

# ---- Fallback OCR (no paddlepaddle dep, ONNX-based) ----
rapidocr-onnxruntime>=1.3

# ---- Last-resort OCR (needs system Tesseract binary) ----
pytesseract>=0.3.13
pdf2image>=1.17
